---
phase: 02-reading-experience-pdf-viewer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/screens/burdah_reader_screen.dart
  - lib/widgets/pdf_page_swiper.dart
  - lib/widgets/zoomable_pdf_page.dart
  - lib/screens/design_system_test_screen.dart
autonomous: false
requirements:
  - READ-01
  - READ-02
  - READ-03
  - READ-04
  - DSGN-04
  - ARCH-01

must_haves:
  truths:
    - "User can swipe through all 56 pages of the bundled Burdah PDF one page at a time (READ-01)"
    - "Pinch-to-zoom on any page locks swipe navigation; releasing zoom back to 1.0x restores swipe (READ-02)"
    - "PDF pages render as rasterized images via PDFium — no text extraction, original Arabic calligraphy and layout preserved exactly as authored (READ-03)"
    - { statement: "A corrupted or unreadable PDF asset shows the established error-state UI copy rather than crashing (READ-03 edge: empty/null input)", verification: backstop }
    - { statement: "PDF content is rendered as a page image — no byte/codepoint/grapheme text interpretation is applied to the document (READ-03 edge: encoding)", verification: backstop }
    - "Page-turn direction follows RTL convention — PageView.reverse is true so page 1 appears first and forward swiping moves right-to-left (READ-04)"
    - "Reader screen displays Islamic-themed chrome using Phase 1 design system — styled AppBar with BurdahColors, geometric accents outside the gesture-active viewport (DSGN-04)"
    - "PDF loads exclusively from the bundled asset path via PdfDocumentViewBuilder.asset with zero network calls (ARCH-01)"
  artifacts:
    - "lib/screens/burdah_reader_screen.dart"
    - "lib/widgets/pdf_page_swiper.dart"
    - "lib/widgets/zoomable_pdf_page.dart"
    - "pubspec.yaml contains pdfrx dependency"
  key_links:
    - "Burdah.pdfAsset string → PdfDocumentViewBuilder.asset → PdfPageView renders each page of the bundled PDF"
    - "TransformationController scale > 1.01 → onZoomChanged(true) callback → PdfPageSwiper sets PageView.physics to NeverScrollableScrollPhysics; scale resets to 1.0 → onZoomChanged(false) → physics restored to PageScrollPhysics"
    - "BurdahReaderScreen constructor accepts a Burdah parameter — ready for Phase 3 go_router wiring without rework"
    - "Design system test screen GoldCtaButton navigates to BurdahReaderScreen — temporary entry point until Phase 3 replaces with go_router"
  prohibitions: []
  # Prohibition probe ran (spec-less fallback, Phase 2). Adversarial recall produced
  # candidates around text extraction and content corruption. All dropped via canon-referral:
  # READ-03 ("faithful rendering, not extracted text") and REQUIREMENTS.md Out of Scope
  # ("Text extraction from PDF — accuracy uncertain") already canonically forbid these.
  # No descriptor-less prohibitions to author.
---

<objective>
Deliver the complete PDF reading experience for the Burdah of Sayyida Khadija RA — page-by-page swipe navigation, pinch-to-zoom with swipe-lock, RTL page order, Islamic-themed reader chrome, and offline-only asset loading — as a single vertical slice from dependency installation through interactive verification. Addresses READ-01, READ-02, READ-03, READ-04, DSGN-04, ARCH-01.

Purpose: This is the app's core value — the reading experience. Everything else (navigation, splash) exists to get users here and make the arrival feel reverent. The PDF reader must render the original document with pixel fidelity via PDFium, not text extraction, honoring the sacred nature of the Arabic calligraphy.

Output: Three new Dart files (reader screen, page swiper widget, zoomable page widget), pdfrx dependency added, temporary navigation wired from the Phase 1 test screen for verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-architecture-design-system/01-01-SUMMARY.md
@.planning/phases/02-reading-experience-pdf-viewer/02-RESEARCH.md
@.planning/phases/02-reading-experience-pdf-viewer/02-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end PDF reading — swipe, zoom-lock, RTL, Islamic chrome</name>
  <files>pubspec.yaml, lib/widgets/zoomable_pdf_page.dart, lib/widgets/pdf_page_swiper.dart, lib/screens/burdah_reader_screen.dart, lib/screens/design_system_test_screen.dart</files>

  <read_first>
    pubspec.yaml — current dependencies; add pdfrx here
    lib/data/models/burdah.dart — Burdah class shape (id, title, titleArabic, pdfAsset, sortOrder); constructor param for the reader screen
    lib/screens/design_system_test_screen.dart — StatefulWidget scaffold pattern, theme consumption (Theme.of(context).extension BurdahColors), error-state copy contract ("Something's not right"), EdgeInsetsDirectional convention, Directionality wrap for Arabic text; also the GoldCtaButton onPressed to wire temporary navigation
    lib/widgets/geometric_border_frame.dart — doc-comment convention (cite requirement IDs), static const naming (_contentInset style), colorFilter pattern; confirms 24px content inset and Stack/Positioned.fill structure that must NOT wrap the gesture-active PageView
    lib/theme/app_theme_extension.dart — BurdahColors extension (gold, borderStroke, cream); access pattern for reader chrome
    lib/widgets/gold_cta_button.dart — callback-driven constructor convention (required named params + VoidCallback)
    .planning/phases/02-reading-experience-pdf-viewer/02-RESEARCH.md — Architecture Patterns 1-3, Anti-Patterns, Common Pitfalls 1-4, Open Question 1 (verify PdfPageView zoom interaction), Assumptions A1/A4
    .planning/phases/02-reading-experience-pdf-viewer/02-PATTERNS.md — Pattern assignments for all three new files
  </read_first>

  <action>
    1. Add pdfrx dependency: run `flutter pub add pdfrx` from the project root. Confirm it resolves without conflicts by checking `flutter pub get` exits cleanly.

    2. Create lib/widgets/zoomable_pdf_page.dart — a StatefulWidget (READ-02) that:
       - Accepts required named params: document (PdfDocument from pdfrx), pageNumber (int), onZoomChanged (ValueChanged of bool).
       - Owns a TransformationController in State; in initState, adds a listener that reads controller.value.getMaxScaleOnAxis() and calls widget.onZoomChanged(scale > 1.01) — the 1.01 epsilon avoids float jitter per RESEARCH.md Pattern 2.
       - Disposes the controller in dispose().
       - Builds an InteractiveViewer with transformationController set to the controller, minScale 1.0, maxScale 4.0, wrapping a PdfPageView with the document and pageNumber.
       - IMPORTANT: Per RESEARCH.md Open Question 1 / Assumption A1, verify at implementation time that PdfPageView does not have built-in zoom that conflicts with the outer InteractiveViewer. If pdfrx 2.4.7's PdfPageView exposes its own zoom behavior, use that instead of wrapping in InteractiveViewer, and adapt the scale-tracking to whatever API pdfrx provides. The swipe-lock callback contract (onZoomChanged) stays the same either way.
       - Doc-comment cites READ-02 and explains the per-page zoom independence (each page gets its own TransformationController, never shared, per RESEARCH.md Pitfall 1).
       - Follow the constructor convention from gold_cta_button.dart (required named params with doc-comment).

    3. Create lib/widgets/pdf_page_swiper.dart — a StatefulWidget (READ-01, READ-04) that:
       - Accepts required named param: pdfAsset (String — the asset path from Burdah.pdfAsset).
       - Owns a bool swipeLocked state field (initially false) and a PageController.
       - Resets swipeLocked to false on page change via the PageController or onPageChanged callback (per RESEARCH.md Pitfall 1 — zoom-lock state must not leak across pages).
       - Builds a PdfDocumentViewBuilder.asset(pdfAsset, builder: ...) wrapping a PageView.builder with:
         - reverse: true (RTL page order per READ-04 / RESEARCH.md Pattern 3)
         - controller: the PageController
         - itemCount: document.pages.length (guard null document with 0)
         - physics: swipeLocked ? NeverScrollableScrollPhysics() : PageScrollPhysics()
         - itemBuilder builds a ZoomablePdfPage for each index, passing document, pageNumber (index + 1), and an onZoomChanged callback that sets swipeLocked via setState.
       - Handles the document-loading error state: if PdfDocumentViewBuilder's builder receives a null document and the loading is complete, show the established error-state copy from design_system_test_screen.dart ("Something's not right" / "This couldn't be loaded...") using the same textTheme and colorScheme.error pattern — per RESEARCH.md Security Domain V5.
       - Doc-comment cites READ-01, READ-04, and explains the PageView.builder (not PdfViewer) choice per RESEARCH.md Architecture Patterns.
       - Uses EdgeInsetsDirectional for any padding. No hardcoded left/right.
       - Private static consts for magic numbers following the _contentInset naming pattern from geometric_border_frame.dart.

    4. Create lib/screens/burdah_reader_screen.dart — a StatelessWidget (DSGN-04) that:
       - Accepts required named param: burdah (Burdah from lib/data/models/burdah.dart). This makes it Phase 3 go_router-ready per RESEARCH.md Architectural Responsibility Map.
       - Builds a Scaffold with a themed AppBar:
         - Title shows burdah.title (English) in the app's text theme.
         - AppBar background uses Theme.of(context).colorScheme.surface or the app's existing scaffold color.
         - If burdah.titleArabic is non-null, show it as a subtitle wrapped in Directionality(textDirection: TextDirection.rtl, ...) per the existing pattern in design_system_test_screen.dart.
       - Body contains the PdfPageSwiper(pdfAsset: burdah.pdfAsset) widget taking the full available space.
       - CRITICAL per RESEARCH.md Anti-Patterns: Do NOT wrap PdfPageSwiper inside GeometricBorderFrame. The geometric border must NOT overlay the gesture-active PDF viewport. Instead, apply Islamic chrome through the styled AppBar (using BurdahColors.gold for accents or borderStroke for dividers) and leave the PDF content area fully unencumbered for swipe/pinch gestures.
       - Uses SafeArea to avoid system UI overlap.
       - Doc-comment cites DSGN-04 and explains the chrome-outside-viewport choice.
       - All padding uses EdgeInsetsDirectional. No hardcoded left/right.

    5. Update lib/screens/design_system_test_screen.dart — wire the existing GoldCtaButton onPressed to navigate to BurdahReaderScreen:
       - Import burdah_reader_screen.dart.
       - In the GoldCtaButton onPressed callback (currently a print statement), replace with: load the first Burdah from the already-fetched _burdahsFuture (the FutureBuilder already has it) and Navigator.push to BurdahReaderScreen(burdah: firstBurdah).
       - Since the button is outside the FutureBuilder, the simplest approach: store the loaded burdahs list in a state field (set from the FutureBuilder's snapshot.data) and use it in onPressed, guarding against null/empty.
       - Keep the existing print statement as a fallback if burdahs haven't loaded yet.

    6. Run `flutter analyze` — must report zero issues.
  </action>

  <verify>
    <automated>cd /Users/0xnormii/Desktop/BurdahApp && flutter analyze</automated>
  </verify>

  <acceptance_criteria>
    - flutter analyze reports zero issues (no errors, no warnings, no hints from project code)
    - pubspec.yaml contains a pdfrx dependency entry (grep -c 'pdfrx' pubspec.yaml returns >= 1)
    - lib/widgets/zoomable_pdf_page.dart exists and contains: StatefulWidget class, TransformationController, onZoomChanged callback parameter, InteractiveViewer widget (or pdfrx-native zoom equivalent), PdfPageView usage, dispose() method
    - lib/widgets/pdf_page_swiper.dart exists and contains: PdfDocumentViewBuilder.asset call, PageView.builder with reverse: true, NeverScrollableScrollPhysics conditional, onPageChanged or PageController listener that resets zoom-lock state, error-state UI with "Something's not right" copy
    - lib/screens/burdah_reader_screen.dart exists and contains: Burdah constructor parameter, AppBar with burdah.title, PdfPageSwiper usage, SafeArea, no GeometricBorderFrame wrapping the PDF viewport
    - lib/screens/design_system_test_screen.dart GoldCtaButton onPressed navigates to BurdahReaderScreen (contains Navigator.push and BurdahReaderScreen)
    - No file in this plan contains hardcoded EdgeInsets.only(left: or right: — all directional padding uses EdgeInsetsDirectional
  </acceptance_criteria>

  <done>
    The complete PDF reading experience compiles without analyzer issues: a user can tap "Read Burdah" on the test screen to open BurdahReaderScreen, swipe through all 56 pages with RTL page order, pinch-to-zoom on any page (which locks swiping), release zoom to resume swiping, all rendered via PDFium from the bundled asset with Islamic-styled app bar chrome and proper error handling for load failures.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify PDF reading experience on emulator</name>
  <what-built>Complete PDF reading experience for the Burdah of Sayyida Khadija RA: page-by-page swipe navigation, pinch-to-zoom with swipe-lock, RTL page order, Islamic-themed reader chrome, offline PDF rendering via PDFium.</what-built>
  <how-to-verify>
    1. Launch the app on the Android emulator: `flutter run`
    2. On the Design System Test screen, tap the gold "Read Burdah" button at the bottom
    3. Verify the Burdah Reader screen opens with:
       - An AppBar showing "Burdah of Sayyida Khadija RA" (English title)
       - Arabic subtitle visible if present
       - Islamic-styled chrome on the app bar (themed colors, no plain default Material look)
    4. Verify PDF rendering (READ-03):
       - Page 1 of the Burdah should be visible
       - Arabic calligraphy should be sharp and faithful to the original document
       - No text appears to be "extracted" or re-rendered — it looks like the actual PDF page
    5. Verify swipe navigation (READ-01, READ-04):
       - Swipe left — this should advance to the next page (RTL: forward = left swipe)
       - Swipe right — this should go back to the previous page
       - Confirm this feels natural for Arabic book reading (page 1 on right, progressing leftward)
       - If the direction feels BACKWARDS (forward swipe goes right instead of left), report this — the `reverse: true` flag may need adjustment
    6. Verify pinch-to-zoom (READ-02):
       - Pinch to zoom in on the current page — text should enlarge clearly
       - While zoomed in, try swiping left/right — swiping should be LOCKED (page should NOT turn)
       - Pinch back out to normal size (1x) — swiping should work again
       - Navigate to a different page, confirm zoom state resets (new page starts at 1x, not zoomed)
    7. Verify offline (ARCH-01):
       - Enable airplane mode on the emulator
       - Navigate back and re-open the reader — PDF should still load and render
    8. Verify error handling:
       - (Optional) If you can simulate a load failure, confirm the app shows "Something's not right" rather than crashing
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. If any issue found, describe what happened (especially the RTL swipe direction — this is the highest-risk item per RESEARCH.md Pitfall 2).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Asset bundle → PDFium native renderer | Bundled PDF file bytes cross from Flutter asset loader into native PDFium C++ parser for rasterization |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Denial of Service | PdfDocumentViewBuilder.asset / PdfPageView | low | mitigate | Wrap PDF document loading in error handling; show established error-state UI copy ("Something's not right") on load failure rather than letting an uncaught exception crash the screen. Risk is LOW because the PDF is bundled at build time (developer-controlled, not user-supplied). |
| T-02-02 | Tampering | pdfrx native PDFium bindings | low | accept | Native PDFium memory-safety vulnerabilities are out of this project's direct control. Mitigated upstream by staying current on pdfrx version updates (which bundle PDFium updates). Not exploitable via user-supplied input in v1 since the only PDF loaded is the bundled, developer-controlled asset. |
| T-02-SC | Tampering | flutter pub add pdfrx | low | mitigate | pdfrx 2.4.7 verified in RESEARCH.md Package Legitimacy Audit: publisher espresso3389.jp verified on pub.dev, MIT-style license, 336 likes / 160 pub points, already approved in Phase 1 audit. No blocking checkpoint needed — package is OK/Approved. |
</threat_model>

## Artifacts this phase produces

**New files:**
- `lib/screens/burdah_reader_screen.dart` — `BurdahReaderScreen` StatelessWidget class, accepts `Burdah` constructor param
- `lib/widgets/pdf_page_swiper.dart` — `PdfPageSwiper` StatefulWidget class, accepts `pdfAsset` String param; `_PdfPageSwiperState` with `swipeLocked` bool field
- `lib/widgets/zoomable_pdf_page.dart` — `ZoomablePdfPage` StatefulWidget class, accepts `PdfDocument document`, `int pageNumber`, `ValueChanged<bool> onZoomChanged` params; `_ZoomablePdfPageState` with `TransformationController _controller`

**Modified files:**
- `pubspec.yaml` — new dependency entry `pdfrx: ^2.4.7` (or resolved version from `flutter pub add`)
- `lib/screens/design_system_test_screen.dart` — `GoldCtaButton.onPressed` updated from print-only to `Navigator.push` to `BurdahReaderScreen`; new import for `burdah_reader_screen.dart`; state field to hold loaded `Burdah` list for button access

**New symbols:**
- `BurdahReaderScreen` (StatelessWidget)
- `PdfPageSwiper` (StatefulWidget), `_PdfPageSwiperState`
- `ZoomablePdfPage` (StatefulWidget), `_ZoomablePdfPageState`

**No new CLI flags, decorators, struct/dataclass fields, or configuration keys.**

<verification>
1. `flutter analyze` — zero issues
2. `grep -c 'pdfrx' pubspec.yaml` — returns >= 1
3. All three new files exist under lib/widgets/ and lib/screens/
4. Reader screen opens from test screen button and displays PDF pages
5. Swipe, zoom-lock, and RTL direction verified by human checkpoint
</verification>

<success_criteria>
- User can open the Burdah reader from the test screen, swipe through all 56 pages of the PDF
- Pinch-to-zoom works on any page and locks swipe; zoom reset releases the lock
- Page-turn direction matches RTL Arabic book convention (human-verified)
- Reader chrome uses the Phase 1 design system colors (not default Material)
- PDF renders offline from bundled assets with no network dependency
- Load failures show error UI instead of crashing
- flutter analyze passes with zero issues
</success_criteria>

<output>
Create `.planning/phases/02-reading-experience-pdf-viewer/02-01-SUMMARY.md` when done
</output>
