---
phase: 03-navigation-primary-user-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/main.dart
  - lib/app.dart
  - lib/router/app_router.dart
  - lib/screens/home_screen.dart
  - lib/screens/burdah_list_screen.dart
  - lib/widgets/burdah_list_row.dart
  - lib/screens/burdah_reveal_screen.dart
  - lib/widgets/transitional_reveal_image.dart
  - lib/data/models/burdah.dart
  - assets/data/burdah_catalog.json
  - pubspec.yaml
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03]

must_haves:
  truths:
    - "User sees HomeScreen with a single centered GoldCtaButton labelled 'Burdah' on themed background, no other chrome (NAV-01, D-02, D-03)"
    - "Tapping the Burdah button navigates to BurdahListScreen showing burdah entries loaded from BurdahRepository.getAll() via Provider DI (NAV-02)"
    - "Tapping a burdah list entry triggers a 2-second transitional reveal image with fade-in, glow-to-peak, fade-out before the reader opens (D-05, D-09)"
    - "Back navigation from BurdahReaderScreen returns to BurdahListScreen without replaying the reveal image (NAV-03, D-07)"
    - "Back navigation from BurdahListScreen returns to HomeScreen (NAV-03)"
    - "All screen-to-screen transitions use FadeTransition via CustomTransitionPage, not the default Material slide (D-08)"
    - "BurdahRepository is provided via Provider at the app root and accessed via context.read<BurdahRepository>() — no direct AssetBurdahRepository instantiation in screens"
  prohibitions:
    - "HomeScreen must NOT contain AppBar title text, app name, Arabic calligraphy header, or geometric framing (D-01, D-02)"
    - "BurdahListScreen must NOT use card frames or geometric decoration for burdah entries (D-01, D-04)"
    - "Route parameters must NOT be interpolated directly into asset file paths — always resolve through BurdahRepository.getById() (RESEARCH Pattern 2)"
    - "GeometricBorderFrame and GeometricCardFrame widgets must NOT appear on any new screen (D-01)"
    - "context.go() must NOT be used for forward navigation — use context.push() and pushReplacement() exclusively (RESEARCH Anti-Patterns)"
  artifacts:
    - path: "lib/router/app_router.dart"
      provides: "GoRouter configuration with 4 fade-transitioned routes"
      contains: "GoRouter"
    - path: "lib/screens/home_screen.dart"
      provides: "Minimal home screen with centered Burdah CTA"
      contains: "HomeScreen"
    - path: "lib/screens/burdah_list_screen.dart"
      provides: "Burdah list screen sourced from repository"
      contains: "BurdahListScreen"
    - path: "lib/screens/burdah_reveal_screen.dart"
      provides: "Transitional reveal screen with glow animation"
      contains: "BurdahRevealScreen"
    - path: "lib/widgets/burdah_list_row.dart"
      provides: "Simple list row for burdah entries"
      contains: "BurdahListRow"
    - path: "lib/widgets/transitional_reveal_image.dart"
      provides: "Reusable glow-fade animation widget"
      contains: "TransitionalRevealImage"
  key_links:
    - from: "lib/app.dart"
      to: "lib/router/app_router.dart"
      via: "MaterialApp.router(routerConfig: appRouter)"
      pattern: "routerConfig.*appRouter"
    - from: "lib/main.dart"
      to: "lib/data/repositories/burdah_repository.dart"
      via: "Provider<BurdahRepository> wrapping BurdahApp at app root"
      pattern: "Provider<BurdahRepository>"
    - from: "lib/screens/burdah_list_screen.dart"
      to: "lib/router/app_router.dart"
      via: "context.push for forward navigation on list row tap"
      pattern: "context\\.push"
    - from: "lib/screens/burdah_reveal_screen.dart"
      to: "lib/screens/burdah_reader_screen.dart"
      via: "context.pushReplacement swaps reveal for reader after animation"
      pattern: "pushReplacement"
---

<objective>
Wire the complete Home -> Burdah List -> Reader navigation flow end-to-end using go_router, with Provider-based DI, gentle fade transitions (D-08), and a transitional reveal image experience (D-05/D-09) before the reader opens. Implements NAV-01, NAV-02, NAV-03, D-01 through D-09.

Purpose: Connect the Phase 1 catalog/design system with the Phase 2 reader into a complete, navigable app flow that a user can walk through from launch to reading and back.
Output: 6 new files (router, 3 screens, 2 widgets), 5 modified files (main.dart, app.dart, burdah.dart, catalog JSON, pubspec.yaml), 1 moved asset.
</objective>

## Phase Goal

**As a** reader, **I want to** navigate from Home through a Burdah list to the PDF reader and back, **so that** I can discover and read any available burdah poem in a complete, connected app flow.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-primary-user-flow/03-RESEARCH.md
@.planning/phases/03-navigation-primary-user-flow/03-PATTERNS.md
@.planning/phases/02-reading-experience-pdf-viewer/02-01-SUMMARY.md
</context>

## Artifacts this phase produces

**New files:**
- `lib/router/app_router.dart` — exports top-level `appRouter` (GoRouter instance) with 4 routes
- `lib/screens/home_screen.dart` — exports `HomeScreen` (StatelessWidget)
- `lib/screens/burdah_list_screen.dart` — exports `BurdahListScreen` (StatefulWidget)
- `lib/screens/burdah_reveal_screen.dart` — exports `BurdahRevealScreen` (StatelessWidget, accepts `burdahId` param)
- `lib/widgets/burdah_list_row.dart` — exports `BurdahListRow` (StatelessWidget, accepts `burdah` + `onTap`)
- `lib/widgets/transitional_reveal_image.dart` — exports `TransitionalRevealImage` (StatelessWidget, accepts `assetPath` + `onComplete`)
- `assets/images/khadija_resting_place.jpeg` — moved and renamed from project root `IMG_1131.jpeg`

**New symbols in existing files:**
- `Burdah.transitionImageAsset` — `String?` field added to `lib/data/models/burdah.dart`
- `BurdahReaderLoader` — StatelessWidget defined inline in `app_router.dart` (FutureBuilder wrapper resolving burdah ID via repository)

**Modified files:**
- `lib/main.dart` — wraps `BurdahApp` in `Provider<BurdahRepository>(create: (_) => AssetBurdahRepository())`
- `lib/app.dart` — switches from `MaterialApp(home:)` to `MaterialApp.router(routerConfig: appRouter)`
- `lib/data/models/burdah.dart` — adds optional `transitionImageAsset` field to constructor and `fromJson`
- `assets/data/burdah_catalog.json` — adds `transitionImageAsset` value to khadija-ra entry
- `pubspec.yaml` — adds `flutter_animate` dependency, registers `assets/images/` directory

<tasks>

<task type="tracer">
  <name>End-to-end Home -> List -> Reader navigation with go_router, Provider DI, and fade transitions</name>
  <read_first>
    - lib/main.dart (current entry point — 12 lines, need to see GoogleFonts.config line placement)
    - lib/app.dart (current MaterialApp wiring — 27 lines, need exact constructor to replace)
    - lib/screens/burdah_reader_screen.dart (Phase 2 reader — verify constructor accepts Burdah, confirm import path)
    - lib/widgets/gold_cta_button.dart (GoldCtaButton constructor contract — label + onPressed params)
    - lib/data/models/burdah.dart (Burdah model — see existing fields and fromJson pattern)
    - lib/data/repositories/burdah_repository.dart (abstract interface — getAll() and getById() signatures)
    - lib/data/repositories/asset_burdah_repository.dart (concrete impl — verify class name)
    - lib/screens/design_system_test_screen.dart (FutureBuilder/error-state/empty-state copy patterns to reuse, lines 81-138 for the three-state builder, lines 100-116 for error copy)
    - lib/theme/app_theme.dart (AppTheme.light() and AppTheme.dark() factory method names)
    - .planning/phases/03-navigation-primary-user-flow/03-PATTERNS.md (exact patterns and code excerpts to follow)
  </read_first>
  <files>
    lib/main.dart,
    lib/app.dart,
    lib/router/app_router.dart,
    lib/screens/home_screen.dart,
    lib/screens/burdah_list_screen.dart,
    lib/widgets/burdah_list_row.dart
  </files>
  <action>
    1. Modify lib/main.dart: wrap the existing `runApp(const BurdahApp())` call in `Provider&lt;BurdahRepository&gt;(create: (_) => AssetBurdahRepository(), child: const BurdahApp())`. Add imports for `package:provider/provider.dart`, `data/repositories/burdah_repository.dart`, and `data/repositories/asset_burdah_repository.dart`. Keep `GoogleFonts.config.allowRuntimeFetching = false` BEFORE the `runApp` call — do not move or remove it. Per D-01 through D-03.

    2. Modify lib/app.dart: switch from `MaterialApp(home: const DesignSystemTestScreen())` to `MaterialApp.router(routerConfig: appRouter)`. Remove the `design_system_test_screen.dart` import. Add `import 'router/app_router.dart';`. Keep `title`, `debugShowCheckedModeBanner`, `theme` (AppTheme.light()), `darkTheme` (AppTheme.dark()), and `themeMode` (ThemeMode.system) exactly as they are. This is RESEARCH.md Pitfall 1 — the swap from MaterialApp to MaterialApp.router is the critical integration point. Per D-08.

    3. Create lib/router/app_router.dart: define a top-level `final appRouter = GoRouter(...)` with `initialLocation: '/'` and three routes for this tracer slice. Each route uses `pageBuilder` returning `CustomTransitionPage` with `FadeTransition(opacity: animation, child: child)` as the `transitionsBuilder` and `transitionDuration: const Duration(milliseconds: 400)` to satisfy D-08 gentle fades (not the default 300ms Material slide). Routes:
       - `path: '/'` renders `HomeScreen()`
       - `path: '/burdahs'` renders `BurdahListScreen()`
       - `path: '/burdahs/:id'` renders `BurdahReaderLoader(burdahId: state.pathParameters['id']!)`
       Define `BurdahReaderLoader` as a private or package-private StatelessWidget in the same file: it uses `context.read&lt;BurdahRepository&gt;().getById(burdahId)` in a `FutureBuilder&lt;Burdah?&gt;`. On `connectionState != done`, show `CircularProgressIndicator`. On `snapshot.data == null`, show the established error-state copy pattern from design_system_test_screen.dart lines 100-116. On success, return `BurdahReaderScreen(burdah: burdah)`. Use `context.push()` for ALL forward navigation — never `context.go()` (RESEARCH Anti-Patterns). Import go_router, provider, Burdah model, BurdahRepository, and BurdahReaderScreen.

    4. Create lib/screens/home_screen.dart: a StatelessWidget with a `Scaffold` (no AppBar — per D-02, no title text or header elements). Body is a `Center` child containing only `GoldCtaButton(label: 'Burdah', onPressed: () => context.push('/burdahs'))`. Background color comes from the theme (Scaffold default or `Theme.of(context).scaffoldBackgroundColor`) — do not hardcode hex values. Import go_router for `context.push` extension, and gold_cta_button.dart. Per D-02, D-03, NAV-01.

    5. Create lib/screens/burdah_list_screen.dart: a StatefulWidget. In `initState`, call `context.read&lt;BurdahRepository&gt;().getAll()` and store the future. In `build`, use `FutureBuilder&lt;List&lt;Burdah&gt;&gt;` with the three-state pattern from design_system_test_screen.dart:
       - Loading: centered CircularProgressIndicator
       - Error: the established error copy block from design_system_test_screen.dart lines 100-116
       - Empty: the established empty copy block from design_system_test_screen.dart lines 119-137
       - Success: `ListView.separated` over the burdah list, rendering each entry as `BurdahListRow(burdah: burdah, onTap: () => context.push('/burdahs/${burdah.id}/reveal'))`. Note: for now in this tracer task, route the tap to `/burdahs/${burdah.id}` directly (the /reveal route is added in the expansion task). Include a simple AppBar with a title like 'Burdah Collection' or just the app context — Claude decides the exact title text that fits the minimal aesthetic. Per D-04, NAV-02.

    6. Create lib/widgets/burdah_list_row.dart: a StatelessWidget accepting `Burdah burdah` and `VoidCallback onTap`. Renders a `ListTile` (not a Card — D-04) with `onTap`, `title` showing `burdah.titleArabic` wrapped in `Directionality(textDirection: TextDirection.rtl)` if non-null (falling back to `burdah.title`), and `subtitle` showing `burdah.title` in `textTheme.bodyMedium`. Follow the exact RTL text pattern from design_system_test_screen.dart lines 172-178 and the RESEARCH.md BurdahListRow code example. Per D-04.

    7. Run `flutter analyze` — must report zero issues. Run `flutter build apk --debug` to confirm the app compiles.
  </action>
  <verify>
    <automated>cd /Users/0xnormii/Desktop/BurdahApp && flutter analyze 2>&1 | tail -5</automated>
    <automated>grep -c 'MaterialApp.router' lib/app.dart</automated>
    <automated>grep -c 'Provider' lib/main.dart</automated>
    <automated>grep -c 'GoRouter' lib/router/app_router.dart</automated>
    <automated>grep -c 'FadeTransition' lib/router/app_router.dart</automated>
    <automated>grep -c 'GoldCtaButton' lib/screens/home_screen.dart</automated>
    <automated>grep -c 'BurdahListRow' lib/screens/burdah_list_screen.dart</automated>
    <automated>grep -c 'ListTile' lib/widgets/burdah_list_row.dart</automated>
    <automated>grep -v '^//' lib/router/app_router.dart | grep -c 'context\.go('</automated>
  </verify>
  <acceptance_criteria>
    - lib/main.dart contains `Provider&lt;BurdahRepository&gt;` wrapping `BurdahApp` and retains `GoogleFonts.config.allowRuntimeFetching = false` before `runApp`
    - lib/app.dart uses `MaterialApp.router(routerConfig: appRouter)` — NOT `MaterialApp(home:)`
    - lib/app.dart does NOT import design_system_test_screen.dart
    - lib/router/app_router.dart defines `appRouter` as a `GoRouter` with `initialLocation: '/'` and 3 routes (/, /burdahs, /burdahs/:id)
    - Every route in app_router.dart uses `CustomTransitionPage` with `FadeTransition` — no route uses the default `MaterialPage`
    - BurdahReaderLoader in app_router.dart uses `context.read&lt;BurdahRepository&gt;().getById(burdahId)` in a FutureBuilder — never interpolates `burdahId` into an asset path string
    - BurdahReaderLoader shows the established error copy when burdah is null
    - lib/screens/home_screen.dart renders a Scaffold with no AppBar, body is Center with GoldCtaButton labelled 'Burdah'
    - HomeScreen contains zero references to GeometricBorderFrame, GeometricCardFrame, or any decorative header text
    - lib/screens/burdah_list_screen.dart uses `context.read&lt;BurdahRepository&gt;().getAll()` — not `AssetBurdahRepository()` direct instantiation
    - BurdahListScreen has loading, error, and empty states matching the established copy patterns
    - lib/widgets/burdah_list_row.dart renders a ListTile (not a Card) with RTL Directionality for Arabic title
    - `flutter analyze` reports zero issues
    - Zero occurrences of `context.go(` in app_router.dart (grep -v comments returns 0)
  </acceptance_criteria>
  <done>User can launch the app, see a minimal home screen with a single Burdah button, tap it to see the burdah list, tap an entry to open the PDF reader, and navigate back through the stack to home. All transitions are fade-based. Back button appears automatically on List and Reader screens.</done>
</task>

<task type="auto" tdd="false">
  <name>Expansion: transitional reveal image with flutter_animate, data model extension, and asset wiring</name>
  <reversibility rating="costly">The transitional image pattern is burdah-specific and extensible — each future burdah entry could have its own image. Changing the pattern later touches model, catalog JSON, and reveal screen, but no external contracts break.</reversibility>
  <read_first>
    - lib/router/app_router.dart (just created in Task 1 — need to see exact route list to add the /reveal route)
    - lib/data/models/burdah.dart (need to see current field list and fromJson pattern to extend with transitionImageAsset)
    - assets/data/burdah_catalog.json (need to see current khadija-ra entry to add transitionImageAsset field)
    - pubspec.yaml (need to see current assets: and dependencies: sections to add flutter_animate and assets/images/)
    - .planning/phases/03-navigation-primary-user-flow/03-RESEARCH.md (Pattern 3 — TransitionalRevealImage code, flutter_animate chaining API, timing breakdown 600+800+600=2000ms)
    - .planning/phases/03-navigation-primary-user-flow/03-PATTERNS.md (burdah_reveal_screen.dart and transitional_reveal_image.dart pattern assignments)
  </read_first>
  <files>
    pubspec.yaml,
    lib/data/models/burdah.dart,
    assets/data/burdah_catalog.json,
    lib/screens/burdah_reveal_screen.dart,
    lib/widgets/transitional_reveal_image.dart,
    lib/router/app_router.dart,
    lib/screens/burdah_list_screen.dart
  </files>
  <action>
    1. Run `flutter pub add flutter_animate` to add the dependency. Verify it resolves to ^4.5.x.

    2. Move the image asset: copy `/Users/0xnormii/Desktop/BurdahApp/IMG_1131.jpeg` to `assets/images/khadija_resting_place.jpeg`. Per D-06. Ensure the `assets/images/` directory is registered in pubspec.yaml under `flutter: assets:` (currently only `assets/images/svg/` is listed — add `- assets/images/` as a sibling entry). Then run `flutter pub get` to pick up the new asset registration. This is RESEARCH.md Pitfall 3 — the image will fail to load at runtime if this registration is missed.

    3. Extend lib/data/models/burdah.dart: add `final String? transitionImageAsset;` as an optional field. Add `this.transitionImageAsset` to the constructor as an optional named parameter (mirrors the existing `titleArabic` optional-field pattern). Add `transitionImageAsset: json['transitionImageAsset'] as String?,` in the `fromJson` factory. Do NOT add validation in the required-fields check block — this field must stay optional per ARCH-03 extensibility contract.

    4. Extend assets/data/burdah_catalog.json: add `"transitionImageAsset": "assets/images/khadija_resting_place.jpeg"` to the existing khadija-ra entry object. Per D-05 extensibility — future burdah entries can optionally include their own image or omit the field.

    5. Create lib/widgets/transitional_reveal_image.dart: a StatelessWidget accepting `String assetPath` and `VoidCallback onComplete`. Build method returns `Image.asset(assetPath, fit: BoxFit.cover)` chained with flutter_animate: `.animate(onComplete: (_) => onComplete())` then `.fadeIn(duration: 600.ms, curve: Curves.easeIn)` then `.then()` then `.custom(duration: 800.ms, builder: ...)` with a `ColorFiltered(colorFilter: ColorFilter.mode(Colors.white.withValues(alpha: 0.35 * (1 - (2 * value - 1).abs())), BlendMode.screen))` glow effect, then `.then()` then `.fadeOut(duration: 600.ms, curve: Curves.easeOut)`. Total: 600+800+600 = 2000ms matching D-05 locked 2-second spec. The glow builds to peak brightness at the midpoint of the custom effect and eases back, making it visually distinct from a simple opacity change per D-09. Use `.withValues(alpha:)` not the deprecated `.withOpacity()`.

    6. Create lib/screens/burdah_reveal_screen.dart: a StatelessWidget accepting `String burdahId`. In build, use `context.read&lt;BurdahRepository&gt;().getById(burdahId)` via FutureBuilder&lt;Burdah?&gt; (same pattern as BurdahReaderLoader from Task 1). On null result, show the established error state copy. On success, render a black-background Scaffold (no AppBar) with a full-screen `TransitionalRevealImage(assetPath: burdah.transitionImageAsset!, onComplete: () { if (!context.mounted) return; context.pushReplacement('/burdahs/${burdah.id}'); })`. The `context.mounted` guard is mandatory per RESEARCH.md Pitfall 4 to prevent disposed-widget errors on rapid back-tap during animation. The `pushReplacement` swaps the reveal screen out of the navigation stack so back from reader goes to list, not replay (D-07).

    7. Modify lib/router/app_router.dart: add a 4th route for `path: '/burdahs/:id/reveal'` that renders `BurdahRevealScreen(burdahId: state.pathParameters['id']!)` with the same `CustomTransitionPage` + `FadeTransition` wrapper as the other routes. Import burdah_reveal_screen.dart.

    8. Modify lib/screens/burdah_list_screen.dart: update the list row onTap to navigate to `/burdahs/${burdah.id}/reveal` instead of `/burdahs/${burdah.id}` — so the reveal screen is visited before the reader. If the resolved burdah has no `transitionImageAsset` (null), skip the reveal and navigate directly to `/burdahs/${burdah.id}`.

    9. Run `flutter analyze` — must report zero issues.
  </action>
  <verify>
    <automated>cd /Users/0xnormii/Desktop/BurdahApp && flutter analyze 2>&1 | tail -5</automated>
    <automated>test -f assets/images/khadija_resting_place.jpeg && echo "IMAGE_PRESENT" || echo "IMAGE_MISSING"</automated>
    <automated>grep -c 'transitionImageAsset' lib/data/models/burdah.dart</automated>
    <automated>grep -c 'transitionImageAsset' assets/data/burdah_catalog.json</automated>
    <automated>grep -c 'flutter_animate' pubspec.yaml</automated>
    <automated>grep -c 'assets/images/' pubspec.yaml</automated>
    <automated>grep -c 'TransitionalRevealImage' lib/widgets/transitional_reveal_image.dart</automated>
    <automated>grep -c 'BurdahRevealScreen' lib/screens/burdah_reveal_screen.dart</automated>
    <automated>grep -c 'pushReplacement' lib/screens/burdah_reveal_screen.dart</automated>
    <automated>grep -c 'context.mounted' lib/screens/burdah_reveal_screen.dart</automated>
    <automated>grep -c '/reveal' lib/router/app_router.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_animate` appears in pubspec.yaml dependencies and resolves via flutter pub get
    - assets/images/khadija_resting_place.jpeg exists (127KB JPEG moved from project root IMG_1131.jpeg)
    - pubspec.yaml flutter.assets list includes `- assets/images/` (not just assets/images/svg/)
    - lib/data/models/burdah.dart has `final String? transitionImageAsset;` field, optional constructor param, and `json['transitionImageAsset'] as String?` in fromJson
    - transitionImageAsset is NOT in the required-fields validation block of burdah.dart
    - assets/data/burdah_catalog.json khadija-ra entry contains `"transitionImageAsset": "assets/images/khadija_resting_place.jpeg"`
    - lib/widgets/transitional_reveal_image.dart exports TransitionalRevealImage with assetPath and onComplete params
    - TransitionalRevealImage animation chain sums to 2000ms (fadeIn 600 + custom 800 + fadeOut 600)
    - The custom effect uses ColorFiltered with BlendMode.screen for the glow — not a simple opacity change
    - lib/screens/burdah_reveal_screen.dart resolves burdah via context.read BurdahRepository getById — not raw ID interpolation
    - burdah_reveal_screen.dart contains `context.mounted` guard before pushReplacement call
    - burdah_reveal_screen.dart uses `pushReplacement` (not push) to swap reveal for reader
    - lib/router/app_router.dart has 4 routes total including /burdahs/:id/reveal
    - burdah_list_screen.dart navigates to /burdahs/:id/reveal on row tap (when transitionImageAsset is present)
    - `flutter analyze` reports zero issues
  </acceptance_criteria>
  <done>Tapping a burdah list entry shows a 2-second transitional reveal image (fade-in, brightness glow to peak, fade-out) of the resting place, then the PDF reader opens. Navigating back from the reader returns to the list without replaying the reveal. The data model is extended for future burdahs to each carry their own reveal image.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: verify complete navigation flow on emulator</name>
  <what-built>Complete Home -> Burdah List -> Reader navigation flow with go_router fade transitions, Provider-based repository injection, and a 2-second transitional reveal image (fade-in/glow/fade-out) before the reader opens. All per D-01 through D-09.</what-built>
  <how-to-verify>
    Launch the app on the Android emulator and verify each step:

    1. **Home screen (D-02, D-03, NAV-01):** App launches to a clean screen with ONLY a centered gold "Burdah" button. No app title, no header text, no geometric borders, no decorative elements — just the button on a plain themed background.

    2. **Home -> List transition (D-08):** Tap "Burdah". Screen should FADE to the burdah list (not slide from right). Verify the transition is a gentle fade, not the default Material slide animation.

    3. **Burdah list (D-04, NAV-02):** List shows the Sayyida Khadija RA burdah entry as a simple text row (Arabic title in RTL), not a card. No geometric decoration or card frames.

    4. **List -> Reveal transition (D-05, D-09):** Tap the burdah entry. A transitional image of the resting place should:
       - Fade in (~0.6s)
       - Glow brighter to peak intensity (visually distinct from a flat fade — should feel like a gentle luminance build-up)
       - Fade out (~0.6s)
       - Total duration approximately 2 seconds

    5. **Reader opens (NAV-02):** After the reveal completes, the PDF reader should open automatically showing the Burdah PDF. Verify pages are readable and swipe works.

    6. **Back from Reader (NAV-03, D-07):** Press the back button (AppBar arrow or system gesture). Should return to the burdah LIST — NOT replay the reveal image. Transition should be a gentle fade.

    7. **Back from List (NAV-03):** Press back again. Should return to the home screen with the Burdah button. Transition should be a gentle fade.

    8. **Full round-trip:** Repeat the flow once more (Home -> List -> Reveal -> Reader -> Back -> Back) to confirm it works consistently.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm the navigation flow works correctly, or describe any issues you see.</resume-signal>
</task>

</tasks>

## Source Coverage Audit

| SOURCE   | ID    | Feature/Requirement                                    | Task  | Status  | Notes |
|----------|-------|--------------------------------------------------------|-------|---------|-------|
| GOAL     | —     | Home -> Burdah List -> Reader flow wired end-to-end    | T1+T2 | COVERED |       |
| REQ      | NAV-01| Home screen with prominent Burdah button               | T1    | COVERED | D-01/D-02 override "geometric design" wording — prominent via size/color/centering |
| REQ      | NAV-02| Tap Burdah to open list of available burdah poems      | T1    | COVERED |       |
| REQ      | NAV-03| Back navigation from any screen to previous screen     | T1    | COVERED | go_router automatic back button + system gesture |
| RESEARCH | —     | go_router flat route table with push/pushReplacement   | T1+T2 | COVERED |       |
| RESEARCH | —     | Provider BurdahRepository DI at app root               | T1    | COVERED |       |
| RESEARCH | —     | CustomTransitionPage + FadeTransition on every route   | T1    | COVERED |       |
| RESEARCH | —     | Extensible transitional reveal image (Pattern 3)       | T2    | COVERED |       |
| RESEARCH | —     | flutter_animate for reveal glow animation              | T2    | COVERED |       |
| RESEARCH | —     | Route param -> repository resolution (Pattern 2)       | T1    | COVERED |       |
| RESEARCH | —     | BurdahReaderLoader FutureBuilder wrapper                | T1    | COVERED |       |
| RESEARCH | —     | transitionImageAsset optional field on Burdah model    | T2    | COVERED |       |
| RESEARCH | —     | DesignSystemTestScreen orphaned (Open Question 2)      | T1    | COVERED | Import removed from app.dart; file left in codebase (harmless, still compiles) |
| CONTEXT  | D-01  | Drop geometric border frames from all screens          | T1    | COVERED | No geometric widgets used on any new screen |
| CONTEXT  | D-02  | No app title/header/decorative elements on home        | T1    | COVERED | HomeScreen has no AppBar, no text, just CTA |
| CONTEXT  | D-03  | Home screen minimal: centered GoldCtaButton only       | T1    | COVERED |       |
| CONTEXT  | D-04  | Simple list rows, not cards                            | T1    | COVERED | ListTile, not Card or GeometricCardFrame |
| CONTEXT  | D-05  | Transitional image fade-in/glow/fade-out, 2 seconds   | T2    | COVERED | flutter_animate chain: 600+800+600=2000ms |
| CONTEXT  | D-06  | Image asset is IMG_1131.jpeg, move to assets/          | T2    | COVERED | Moved to assets/images/khadija_resting_place.jpeg |
| CONTEXT  | D-07  | Entry-only experience, back does not replay             | T2    | COVERED | pushReplacement swaps reveal out of stack |
| CONTEXT  | D-08  | All transitions use gentle fades, not Material slide   | T1    | COVERED | CustomTransitionPage + FadeTransition, 400ms |
| CONTEXT  | D-09  | Transitional image visually distinct from regular fade | T2    | COVERED | ColorFiltered glow effect, not simple opacity |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route params -> repository | Raw string `:id` from go_router path parameters crosses into repository data lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | app_router.dart BurdahReaderLoader / BurdahRevealScreen | medium | mitigate | Every route receiving `:id` resolves through `BurdahRepository.getById(id)` — never interpolates raw param into asset path. Null result shows error UI, not a crash. |
| T-03-02 | Denial of Service | burdah_list_screen.dart / app_router.dart | low | mitigate | FutureBuilder error states surface the established error-copy UI pattern. Phase 1 CatalogLoadException/FormatException handling in AssetBurdahRepository already catches malformed JSON. |
| T-03-SC | Tampering | pubspec.yaml (flutter_animate install) | low | accept | Package legitimacy audit passed: flutter_animate is a Flutter Favorite, verified publisher (gskinner.com), 928K downloads/30 days, BSD-3-Clause, 4232 likes. No checkpoint needed. |
</threat_model>

<verification>
1. `flutter analyze` reports zero issues across the entire project
2. `flutter build apk --debug` completes successfully
3. Full navigation round-trip works: Home -> List -> Reveal -> Reader -> Back (to List) -> Back (to Home)
4. All transitions are fade-based (no Material slide-from-right)
5. Reveal image plays the glow animation and does not replay on back navigation
6. Back button (AppBar arrow) appears on List and Reader screens automatically
</verification>

<success_criteria>
- NAV-01: Home screen shows centered Burdah button on plain themed background, no geometric decoration
- NAV-02: Tapping Burdah opens list sourced from BurdahRepository; tapping entry opens reader via reveal
- NAV-03: Back navigation works from every screen (Reader -> List -> Home) using AppBar back arrow and system back gesture
- D-01 through D-09: All nine locked CONTEXT.md decisions implemented as specified
- Zero flutter analyze issues
- Provider DI wired at app root, no direct repository instantiation in screens
</success_criteria>

<output>
Create `.planning/phases/03-navigation-primary-user-flow/03-01-SUMMARY.md` when done
</output>
