---
phase: 01-foundation-data-architecture-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/main.dart
  - lib/app.dart
  - lib/theme/app_colors.dart
  - lib/theme/app_theme_extension.dart
  - lib/theme/app_text_theme.dart
  - lib/theme/app_theme.dart
  - lib/data/models/burdah.dart
  - lib/data/repositories/burdah_repository.dart
  - lib/data/repositories/asset_burdah_repository.dart
  - lib/screens/design_system_test_screen.dart
  - assets/data/burdah_catalog.json
autonomous: false
requirements: [ARCH-02, ARCH-03, DSGN-02, DSGN-03]
user_setup:
  - service: flutter-toolchain
    why: "Mobile development toolchain for cross-platform builds (ARCH-02)"
    env_vars: []
    dashboard_config:
      - task: "Install Xcode from Mac App Store (12+ GB download)"
        location: "Mac App Store -> Search 'Xcode' -> Get/Install"
      - task: "Accept Xcode license and set developer directory"
        location: "Terminal: sudo xcodebuild -license accept && sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer"
      - task: "Install Flutter SDK, Android Studio, CocoaPods via Homebrew"
        location: "Terminal: brew install --cask flutter android-studio && brew install cocoapods"
      - task: "Accept Android SDK licenses"
        location: "Terminal: yes | flutter doctor --android-licenses"
      - task: "Verify complete toolchain"
        location: "Terminal: flutter doctor (all checks should show green or yellow, no red)"

must_haves:
  truths:
    - "Flutter project builds on both Android and iOS targets with zero analysis errors (ARCH-02)"
    - "AssetBurdahRepository.getAll() loads from bundled JSON and returns at least one Burdah with id khadija-ra, title, titleArabic, pdfAsset, and sortOrder fields (ARCH-03)"
    - "Adding a new burdah to the catalog requires only appending a JSON object to burdah_catalog.json with no Dart code changes (ARCH-03)"
    - "Scheherazade New renders Arabic display/heading text with correct letter joining including the Alef-Madda combination, and Amiri renders Arabic body/label text with correct shaping (DSGN-02)"
    - "ThemeData light uses scaffold background #FAF6EA, primary #0A642B, and BurdahColors.gold #C9A227; ThemeData dark uses scaffold background #0A642B, elevated surface #12793A, and BurdahColors.gold #E3C067 (DSGN-03, D-01 through D-04)"
    - "Both light and dark ThemeData are wired as theme and darkTheme in MaterialApp, switching via system setting (D-03)"
    - "GoogleFonts.config.allowRuntimeFetching is set to false in main.dart before runApp — fonts load exclusively from bundled assets (DSGN-02)"
    - "Repository wraps jsonDecode and fromJson calls in error handling, surfacing a typed exception on malformed JSON rather than an unhandled crash"
    - "Font asset files are listed under flutter.assets in pubspec.yaml (not under fonts) and named per Google Fonts weight convention"
    - "[assumption:edge] DSGN-02 empty-font: if bundled font files are absent or corrupted, GoogleFonts with allowRuntimeFetching=false falls back to platform default font rather than crashing"
    - "[assumption:edge] DSGN-02 encoding: Arabic text shaping (grapheme clusters, ligatures, bidirectional layout) is delegated to HarfBuzz via the Flutter text engine — application code does not perform string length, equality, or manipulation operations on Arabic content"
    - "[backstop:ui] Repository getAll() returning an empty list is a valid state — consumers must handle zero entries using the empty-state copy from UI-SPEC Copywriting Contract"
    - "[backstop:ui] Repository load failure surfaces a typed exception — consumers must handle the error using the error-state copy from UI-SPEC Copywriting Contract"
    - "[covered:ui] Long Arabic titles (the Khadija RA burdah full title is a real-world example) are stored without truncation in the Burdah model — display truncation is a widget-layer concern handled per UI-SPEC long-text rule"
  artifacts:
    - path: "lib/data/models/burdah.dart"
      provides: "Burdah data model with fromJson factory"
      contains: "class Burdah"
    - path: "lib/data/repositories/burdah_repository.dart"
      provides: "Abstract repository interface"
      exports: ["BurdahRepository"]
    - path: "lib/data/repositories/asset_burdah_repository.dart"
      provides: "JSON-loading repository implementation via rootBundle"
      contains: "class AssetBurdahRepository implements BurdahRepository"
    - path: "lib/theme/app_theme_extension.dart"
      provides: "Custom color tokens (gold, green, cream) for both themes"
      contains: "class BurdahColors extends ThemeExtension"
    - path: "lib/theme/app_theme.dart"
      provides: "ThemeData assembly for light and dark modes"
      contains: "ThemeData"
    - path: "lib/theme/app_text_theme.dart"
      provides: "TextTheme mapping font families to typography roles"
      contains: "TextTheme"
    - path: "lib/theme/app_colors.dart"
      provides: "Static hex color constants for both themes"
      contains: "class AppColors"
    - path: "assets/data/burdah_catalog.json"
      provides: "Burdah catalog manifest with extensible schema"
      contains: "khadija-ra"
    - path: "lib/screens/design_system_test_screen.dart"
      provides: "Verification screen for fonts, colors, and catalog data"
      contains: "class DesignSystemTestScreen"
  key_links:
    - from: "lib/screens/design_system_test_screen.dart"
      to: "lib/data/repositories/asset_burdah_repository.dart"
      via: "Loads catalog data via AssetBurdahRepository.getAll()"
      pattern: "AssetBurdahRepository"
    - from: "lib/theme/app_theme.dart"
      to: "lib/theme/app_theme_extension.dart"
      via: "Registers BurdahColors.light and BurdahColors.dark in ThemeData.extensions"
      pattern: "BurdahColors"
    - from: "lib/app.dart"
      to: "lib/theme/app_theme.dart"
      via: "MaterialApp consumes light and dark ThemeData"
      pattern: "theme.*darkTheme"
    - from: "lib/main.dart"
      to: "lib/app.dart"
      via: "runApp with GoogleFonts offline config set beforehand"
      pattern: "allowRuntimeFetching"
    - from: "lib/data/repositories/asset_burdah_repository.dart"
      to: "assets/data/burdah_catalog.json"
      via: "rootBundle.loadString reads the bundled JSON manifest"
      pattern: "rootBundle.loadString"
  prohibitions: []
---

<objective>
Walking skeleton for BurdahApp: scaffold the Flutter project, wire the data layer (JSON catalog through repository to typed model), build the complete theme system (light and dark with Islamic green/gold/cream tokens), bundle calligraphic Arabic fonts offline, and prove it all works end-to-end on a test screen — per ARCH-02, ARCH-03, DSGN-02, DSGN-03, and decisions D-01 through D-04.

Purpose: Prove the full architecture — build system, asset loading, data abstraction, theme tokens, Arabic font shaping — works end-to-end before any real screen is built on top of it. Every later phase inherits this foundation.
Output: A running Flutter app on at least one mobile target, showing the design system test screen with real catalog data, correct Arabic calligraphy, and the verified color palette in both light and dark themes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-data-architecture-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-data-architecture-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-data-architecture-design-system/01-PATTERNS.md
@.planning/phases/01-foundation-data-architecture-design-system/01-UI-SPEC.md
@.planning/phases/01-foundation-data-architecture-design-system/SKELETON.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end walking skeleton: Flutter scaffold + data layer + theme system + test screen</name>
  <precondition>Flutter SDK is on PATH and flutter doctor shows no critical (red) errors for at least one build target (Android or iOS). User has completed all user_setup steps.</precondition>
  <reversibility rating="costly">The Burdah JSON catalog schema (field names, nesting shape) and BurdahColors ThemeExtension token names become the contract Phases 2-4 build against — renaming fields later requires updating every consumer, though no data migration is needed since the catalog is a bundled asset.</reversibility>
  <files>pubspec.yaml, lib/main.dart, lib/app.dart, lib/theme/app_colors.dart, lib/theme/app_theme_extension.dart, lib/theme/app_text_theme.dart, lib/theme/app_theme.dart, lib/data/models/burdah.dart, lib/data/repositories/burdah_repository.dart, lib/data/repositories/asset_burdah_repository.dart, lib/screens/design_system_test_screen.dart, assets/data/burdah_catalog.json</files>
  <read_first>
    - .planning/phases/01-foundation-data-architecture-design-system/01-RESEARCH.md (Architecture Patterns section for code templates: Pattern 1 repository, Pattern 2 ThemeExtension, Code Examples for font bundling; Common Pitfalls for Arabic shaping test case and PDF filename renaming)
    - .planning/phases/01-foundation-data-architecture-design-system/01-PATTERNS.md (Pattern Assignments mapping each file to its template source and Shared Patterns for theme access and directionality rules)
    - .planning/phases/01-foundation-data-architecture-design-system/01-UI-SPEC.md (Color section for exact hex values in both themes, Typography section for sizes and weights and font-family mapping, Spacing Scale for layout values, Copywriting Contract for empty/error state text)
    - .planning/phases/01-foundation-data-architecture-design-system/01-CONTEXT.md (Locked decisions D-01 through D-08 plus Claude Discretion resolutions)
  </read_first>
  <action>
    Run flutter create --org com.burdahapp --project-name burdahapp --platforms android,ios in the project root directory (use the dot-form to scaffold around existing files). Add core dependencies via flutter pub add: provider, google_fonts, flutter_svg, go_router. Go_router is added for stack consistency per RESEARCH.md but routing is not configured until Phase 3.

    Create asset directory structure: assets/data/, assets/pdfs/, assets/google_fonts/, assets/images/svg/. Copy the Arabic-named PDF from the project root into assets/pdfs/ with the ASCII filename burdah_khadija_ra.pdf (per RESEARCH.md Pitfall 5 — non-ASCII filenames break cross-platform asset bundling asymmetrically). Do NOT delete the original file.

    Download Scheherazade New (Regular and Bold weights) and Amiri (Regular and Bold weights) TTF files from Google Fonts and place them in assets/google_fonts/ with the exact naming convention: ScheherazadeNew-Regular.ttf, ScheherazadeNew-Bold.ttf, Amiri-Regular.ttf, Amiri-Bold.ttf. The google_fonts package matches on this exact naming pattern to prefer local files over network fetch.

    Register all four asset directories in pubspec.yaml under the flutter.assets key (NOT under fonts — per RESEARCH.md Anti-Patterns, listing google_fonts assets under the fonts section silently breaks asset-priority matching and falls back to network fetch): assets/data/burdah_catalog.json, assets/google_fonts/, assets/images/svg/, assets/pdfs/.

    Create assets/data/burdah_catalog.json with the schema from PATTERNS.md Pattern 1: a top-level burdahs array containing one entry with id khadija-ra, title Burdah of Sayyida Khadija RA, titleArabic field with the Arabic title, pdfAsset pointing to assets/pdfs/burdah_khadija_ra.pdf, and sortOrder 1.

    Create lib/data/models/burdah.dart with the Burdah class per PATTERNS.md: five fields (id String, title String, titleArabic String nullable, pdfAsset String, sortOrder int), const constructor with required/optional params, factory Burdah.fromJson(Map) with typed casts. Wrap the fromJson body so that missing required fields (id, title, pdfAsset) throw a FormatException with a descriptive message rather than a raw type-cast error.

    Create lib/data/repositories/burdah_repository.dart with the abstract BurdahRepository interface: Future getAll returning List of Burdah, Future getById(String id) returning nullable Burdah.

    Create lib/data/repositories/asset_burdah_repository.dart implementing BurdahRepository per PATTERNS.md Pattern 1: load JSON via rootBundle.loadString from assets/data/burdah_catalog.json, decode with jsonDecode, map entries through Burdah.fromJson, sort by sortOrder. Wrap the entire getAll body in a try-catch that catches FormatException and any jsonDecode errors, rethrowing as a single typed exception (e.g., CatalogLoadException with a message field) so consumers can distinguish catalog-load failures from other errors. The getById method calls getAll then uses firstWhere with orElse returning null.

    Create lib/theme/app_colors.dart as a class with static const Color fields for every hex value from UI-SPEC Color section. Light theme: background #FAF6EA, primary green #0A642B, gold accent #C9A227, text primary #1B2B20, text secondary #5B6B5E, text on green #FAF6EA, text on gold #1B2B20, error #B3261E. Dark theme: background #0A642B, elevated surface #12793A, gold accent #E3C067, text primary #F5EFDD, text secondary #C9D6C9, text on gold #1B2B20, error #E2726B. Use Color(0xFF______) format for all values.

    Create lib/theme/app_theme_extension.dart with BurdahColors extending ThemeExtension per PATTERNS.md Pattern 2. Include gold, borderStroke, and cream fields. Provide static const light and dark instances with the locked hex values from UI-SPEC. Implement copyWith and lerp methods for theme animation support.

    Create lib/theme/app_text_theme.dart that builds a TextTheme mapping per UI-SPEC Typography section: Display role at 32px Bold uses GoogleFonts.scheherazadeNew, Heading role at 24px Bold uses GoogleFonts.scheherazadeNew, Body role at 16px Regular line-height 1.6 uses GoogleFonts.amiri, Label role at 14px Regular line-height 1.4 uses GoogleFonts.amiri. Accept a base text color parameter so light/dark themes can pass different text colors.

    Create lib/theme/app_theme.dart with a class providing static methods for light and dark ThemeData. Each ThemeData sets: colorScheme with the correct primary/surface/error colors, scaffoldBackgroundColor, appBarTheme with green background and appropriate text color, textTheme from the text-theme builder with the correct base text color, and extensions registering the corresponding BurdahColors instance. Per D-03, both themes must be fully functional.

    Create lib/main.dart that sets GoogleFonts.config.allowRuntimeFetching = false before calling runApp, per RESEARCH.md Code Examples. Import and instantiate the app widget.

    Create lib/app.dart with a StatelessWidget (BurdahApp) returning MaterialApp with: title BurdahApp, theme set to the light ThemeData, darkTheme set to the dark ThemeData, themeMode ThemeMode.system, home set to DesignSystemTestScreen, and debugShowCheckedModeBanner false.

    Create lib/screens/design_system_test_screen.dart as a StatefulWidget that: (1) Calls AssetBurdahRepository().getAll() in initState via a FutureBuilder or setState pattern and displays the first Burdah entry title and titleArabic. (2) Shows a Scheherazade New font sample section with Arabic text that specifically includes diacritic-heavy content and words containing the Alef-Madda combination — this is the critical verification for Flutter issue 143975 per RESEARCH.md Pitfall 2. (3) Shows an Amiri font sample for the body/label role. (4) Displays color palette swatches: a row of Container widgets filled with the primary green, gold, cream, text-primary, and error colors, each labeled. (5) Uses EdgeInsetsDirectional and AlignmentDirectional throughout — zero hardcoded left/right values per RESEARCH.md Anti-Patterns and CLAUDE.md directionality guardrail. (6) Wraps the FutureBuilder error state with the UI-SPEC error copy heading. The screen is scrollable (SingleChildScrollView) to accommodate all verification sections.

    Run flutter analyze and confirm zero errors. Run the app on at least one target (flutter run on a connected device or emulator) to visually confirm the test screen displays.
  </action>
  <verify>
    <automated>cd /Users/0xnormii/Desktop/BurdahApp && flutter analyze 2>&1 | grep -E 'No issues found|error' | head -5</automated>
  </verify>
  <acceptance_criteria>
    - flutter analyze exits with "No issues found" (zero errors, zero warnings acceptable but zero errors mandatory)
    - assets/data/burdah_catalog.json exists and contains a JSON object with a burdahs array containing at least one entry where id equals khadija-ra
    - lib/data/models/burdah.dart declares class Burdah with a factory constructor named fromJson
    - lib/data/repositories/burdah_repository.dart declares abstract class BurdahRepository with getAll and getById method signatures
    - lib/data/repositories/asset_burdah_repository.dart declares class AssetBurdahRepository implements BurdahRepository
    - lib/theme/app_theme_extension.dart declares class BurdahColors extends ThemeExtension with static const light and static const dark instances
    - lib/theme/app_colors.dart declares a class with static const Color fields including values 0xFF0A642B and 0xFFC9A227 and 0xFFFAF6EA
    - lib/main.dart contains the assignment allowRuntimeFetching = false before the runApp call
    - pubspec.yaml flutter.assets section lists assets/google_fonts/ and assets/data/ and assets/pdfs/ and assets/images/svg/ (all four directories)
    - lib/app.dart provides both theme and darkTheme parameters to MaterialApp
    - lib/screens/design_system_test_screen.dart exists and imports AssetBurdahRepository
    - assets/google_fonts/ directory contains at least ScheherazadeNew-Regular.ttf and Amiri-Regular.ttf
    - assets/pdfs/burdah_khadija_ra.pdf exists (ASCII-renamed copy of the Arabic-filename original)
  </acceptance_criteria>
  <done>App launches on at least one mobile target and displays the design system test screen with: the Khadija RA burdah title loaded from the JSON catalog, Arabic text rendered in Scheherazade New and Amiri with correct letter joining, green/gold/cream color palette swatches, and the full theme system wired for both light and dark modes. flutter analyze reports zero errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Arabic font shaping and color palette on device</name>
  <read_first>
    - .planning/phases/01-foundation-data-architecture-design-system/01-UI-SPEC.md (Color section for expected hex values, Typography section for expected font rendering)
    - .planning/phases/01-foundation-data-architecture-design-system/01-RESEARCH.md (Common Pitfalls section: Pitfall 2 for Scheherazade New ligature-joining bug test case, Pitfall 4 for WCAG contrast numbers)
  </read_first>
  <what-built>Walking skeleton with Flutter scaffold, JSON-driven burdah catalog (repository pattern), complete theme system (light and dark with Islamic green/gold/cream tokens), bundled Scheherazade New and Amiri fonts, and a verification test screen displaying all of the above.</what-built>
  <how-to-verify>
    1. Launch the app: flutter run in the project root (or use a running instance)
    2. FONT CHECK: On the test screen, find the Scheherazade New Arabic text sample. Confirm all Arabic letters are correctly joined (no disconnected letter forms). Look specifically at any words containing lam-alef and alef-madda combinations — if letters appear separated where they should connect, the Flutter issue 143975 shaping bug is present on this Flutter version
    3. AMIRI CHECK: Find the Amiri body-text Arabic sample. Confirm it renders readably at body size with correct letter joining and diacritic marks (dots and vowel marks) properly positioned
    4. PALETTE CHECK: Find the color swatch section. Confirm the green swatch is a deep Islamic green (not teal or lime), the gold swatch is an antique muted gold (not bright yellow), and the background/cream is a warm ivory (not stark white)
    5. DARK THEME CHECK: Switch the device/emulator to dark mode (system settings). Confirm the app switches to a dark theme where the background is the deep green, text is warm off-white, and gold accents are slightly brighter than the light-theme gold
    6. CATALOG DATA CHECK: Confirm the test screen displays the burdah title text loaded from the JSON catalog (should show both English and Arabic title text for Khadija RA)
  </how-to-verify>
  <acceptance_criteria>
    - Scheherazade New Arabic text renders with all letters correctly joined, no disconnected forms visible
    - Amiri body text renders readably with diacritics properly positioned
    - Color palette visually matches: deep green, antique gold, warm ivory
    - Dark theme activates when system dark mode is enabled, with green background and brighter gold
    - Catalog data (Khadija RA burdah title) is visible on the test screen
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed to Plan 02 (geometric border widgets). If Scheherazade New has shaping issues, describe them — the fallback is to use Amiri for display text too (per CONTEXT.md discretion note). If colors need adjustment, describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Asset bundle to Dart runtime | Bundled JSON catalog is parsed by jsonDecode and mapped through Burdah.fromJson — the only input-parsing boundary in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Denial of Service | AssetBurdahRepository.getAll() — malformed bundled JSON causes unhandled crash on app launch | medium | mitigate | Wrap jsonDecode and fromJson in try-catch, surface CatalogLoadException with descriptive message; consumers show UI-SPEC error-state copy instead of crash |
| T-01-02 | Tampering | burdah_catalog.json pdfAsset field — crafted path traversal value | low | accept | Manifest is bundled at build time, not user-editable at runtime; no attacker-controlled input path exists in v1. Revisit if future version allows remote/downloadable catalogs. |
| T-01-SC | Tampering | pub.dev package installs (provider, google_fonts, flutter_svg, go_router) | high | mitigate | Package Legitimacy Audit completed in 01-RESEARCH.md — all packages verified OK against pub.dev with flutter.dev or verified publishers; no ASSUMED or SUS packages |
</threat_model>

<verification>
1. flutter analyze exits with zero errors
2. flutter test runs without crashes (default scaffold test)
3. App launches on at least one mobile target (Android emulator or iOS simulator)
4. Test screen visually displays: catalog data, Arabic fonts with correct shaping, color palette swatches
5. Dark mode toggle (via system settings) switches the full theme
6. Human checkpoint confirms font shaping correctness (blocking — known Flutter bug affects this exact font)
</verification>

<success_criteria>
- ARCH-02: Flutter project builds and runs on at least one mobile target with zero analysis errors
- ARCH-03: Burdah catalog loads from bundled JSON through repository, verified against the Khadija RA entry displaying on the test screen
- DSGN-02: Scheherazade New (display) and Amiri (body) calligraphic Arabic fonts render correctly with proper letter joining and diacritics, verified by human checkpoint
- DSGN-03: Islamic green/gold/cream color palette (per D-01 through D-04) displays correctly in both light and dark themes via ThemeExtension tokens
</success_criteria>

## Artifacts this plan produces

**Classes:** Burdah, BurdahRepository (abstract), AssetBurdahRepository, BurdahColors (ThemeExtension), AppColors, AppTheme, AppTextTheme (or equivalent builder), BurdahApp (MaterialApp widget), DesignSystemTestScreen, CatalogLoadException

**Factory methods / constructors:** Burdah.fromJson(Map), BurdahColors.copyWith(...), BurdahColors.lerp(...)

**Repository methods:** BurdahRepository.getAll() returning Future of List of Burdah, BurdahRepository.getById(String) returning Future of nullable Burdah

**Static constants:** BurdahColors.light, BurdahColors.dark, AppColors hex value fields (background, primary, gold, textPrimary, etc. for both themes)

**New files:** pubspec.yaml (modified), lib/main.dart, lib/app.dart, lib/theme/app_colors.dart, lib/theme/app_theme_extension.dart, lib/theme/app_text_theme.dart, lib/theme/app_theme.dart, lib/data/models/burdah.dart, lib/data/repositories/burdah_repository.dart, lib/data/repositories/asset_burdah_repository.dart, lib/screens/design_system_test_screen.dart, assets/data/burdah_catalog.json, assets/pdfs/burdah_khadija_ra.pdf, assets/google_fonts/ScheherazadeNew-Regular.ttf, assets/google_fonts/ScheherazadeNew-Bold.ttf, assets/google_fonts/Amiri-Regular.ttf, assets/google_fonts/Amiri-Bold.ttf

<output>
Create `.planning/phases/01-foundation-data-architecture-design-system/01-01-SUMMARY.md` when done
</output>
