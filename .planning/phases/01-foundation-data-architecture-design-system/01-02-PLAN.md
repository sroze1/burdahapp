---
phase: 01-foundation-data-architecture-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/widgets/geometric_border_frame.dart
  - lib/widgets/geometric_card_frame.dart
  - lib/widgets/gold_cta_button.dart
  - lib/screens/design_system_test_screen.dart
  - assets/images/svg/star_tessellation_frame_full.svg
  - assets/images/svg/star_tessellation_frame_card.svg
autonomous: false
requirements: [DSGN-01]
user_setup: []

must_haves:
  truths:
    - "GeometricBorderFrame renders a full-screen rectangular border using the star tessellation SVG, recolored to the current theme borderStroke color via flutter_svg colorFilter (DSGN-01, D-05, D-06, D-07)"
    - "GeometricCardFrame renders a card-sized rectangular border using the card-variant star tessellation SVG, recolored to the current theme borderStroke color (DSGN-01, D-08)"
    - "GoldCtaButton renders a filled button using BurdahColors.gold as the background fill and AppColors text-on-gold as the label color, with the label Read Burdah per UI-SPEC Copywriting Contract (D-02)"
    - "All three widgets consume theme tokens exclusively via Theme.of(context).extension of BurdahColors — zero hardcoded hex values in widget files"
    - "All three widgets use EdgeInsetsDirectional and AlignmentDirectional — zero hardcoded left/right values"
    - "Both border widget variants and the CTA button render correctly in both light and dark themes"
    - "Gold CTA button label text is at display or heading size (at least 18px) to satisfy the WCAG large-text threshold, since gold-on-green contrast is 3.0-4.2:1 per RESEARCH.md Pitfall 4"
    - "[assumption:ui-overflow] SVG border frame scales via BoxFit.contain so the star tessellation does not distort or clip at phone aspect ratios — visual verification needed on at least one phone-class and one tablet-class screen size"
  artifacts:
    - path: "lib/widgets/geometric_border_frame.dart"
      provides: "Full-screen geometric star tessellation border widget"
      contains: "class GeometricBorderFrame"
    - path: "lib/widgets/geometric_card_frame.dart"
      provides: "Card-sized geometric star tessellation border widget"
      contains: "class GeometricCardFrame"
    - path: "lib/widgets/gold_cta_button.dart"
      provides: "Gold-filled call-to-action button with theme-driven colors"
      contains: "class GoldCtaButton"
    - path: "assets/images/svg/star_tessellation_frame_full.svg"
      provides: "Full-screen star tessellation SVG pattern asset"
    - path: "assets/images/svg/star_tessellation_frame_card.svg"
      provides: "Card-sized star tessellation SVG pattern asset"
  key_links:
    - from: "lib/widgets/geometric_border_frame.dart"
      to: "lib/theme/app_theme_extension.dart"
      via: "Reads BurdahColors.borderStroke for SVG colorFilter"
      pattern: "BurdahColors"
    - from: "lib/widgets/gold_cta_button.dart"
      to: "lib/theme/app_theme_extension.dart"
      via: "Reads BurdahColors.gold for button fill color"
      pattern: "BurdahColors"
    - from: "lib/screens/design_system_test_screen.dart"
      to: "lib/widgets/geometric_border_frame.dart"
      via: "Test screen wraps content in GeometricBorderFrame to verify rendering"
      pattern: "GeometricBorderFrame"
  prohibitions: []
---

<objective>
Expand the design system with the three reusable themed widgets — two geometric star tessellation border frames (full-screen and card-sized, per D-05 through D-08) and a gold call-to-action button (per D-02) — and integrate them into the test screen for visual verification. This completes DSGN-01 (Islamic geometric patterns throughout) and delivers all four Phase 1 success criteria.

Purpose: Deliver the reusable decorative widgets that every later screen (home, burdah list, PDF reader) will wrap content in, proving they render correctly across both themes before downstream screens depend on them.
Output: Three new widget files, two SVG pattern assets, and an updated test screen showing the full design system — geometric borders framing content, gold CTA button, all themed correctly in both light and dark modes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data-architecture-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-data-architecture-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-data-architecture-design-system/01-PATTERNS.md
@.planning/phases/01-foundation-data-architecture-design-system/01-UI-SPEC.md
@.planning/phases/01-foundation-data-architecture-design-system/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create SVG star tessellation assets, geometric border widgets, and gold CTA button</name>
  <files>assets/images/svg/star_tessellation_frame_full.svg, assets/images/svg/star_tessellation_frame_card.svg, lib/widgets/geometric_border_frame.dart, lib/widgets/geometric_card_frame.dart, lib/widgets/gold_cta_button.dart, lib/screens/design_system_test_screen.dart</files>
  <read_first>
    - .planning/phases/01-foundation-data-architecture-design-system/01-RESEARCH.md (Pattern 3 for SVG recoloring via colorFilter, Pitfall 6 for deprecated color/colorBlendMode params, Pitfall 4 for WCAG gold contrast constraints, Anti-Patterns for directionality rules)
    - .planning/phases/01-foundation-data-architecture-design-system/01-PATTERNS.md (GeometricBorderFrame and GeometricCardFrame pattern assignments, GoldCtaButton pattern, Shared Patterns for theme access and directionality)
    - .planning/phases/01-foundation-data-architecture-design-system/01-UI-SPEC.md (Color section for border stroke and gold accent values in both themes, Spacing Scale for border insets and element gaps, Copywriting Contract for CTA label text)
    - .planning/phases/01-foundation-data-architecture-design-system/01-CONTEXT.md (D-05 border frames only, D-06 interlocking star tessellations 8 or 12-point, D-07 SVG with flutter_svg, D-08 two variants full-screen and card-sized)
    - lib/theme/app_theme_extension.dart (BurdahColors fields: gold, borderStroke, cream — used by all three widgets)
    - lib/theme/app_colors.dart (static color constants for text-on-gold label color)
    - lib/screens/design_system_test_screen.dart (current test screen to be updated with widget showcase)
  </read_first>
  <action>
    Create two SVG files for the star tessellation border frames. Each SVG should define a rectangular border pattern using interlocking 8-point Islamic geometric stars (per D-06). The full-screen variant (star_tessellation_frame_full.svg) should be a larger rectangular frame suitable for wrapping an entire screen, with the star tessellation pattern running along all four edges and corners, leaving the interior empty for content (per D-05 — content area stays clean). The card-sized variant (star_tessellation_frame_card.svg) should be a proportionally smaller frame suitable for wrapping individual cards or list items. Both SVGs should use a single monochrome stroke color (any color — it will be overridden by the colorFilter at runtime) with no fill, so the colorFilter BlendMode.srcIn recoloring works correctly. The tessellation does not need to be geometrically perfect — it needs to be recognizably Islamic geometric art with interlocking star forms.

    Create lib/widgets/geometric_border_frame.dart as a StatelessWidget named GeometricBorderFrame. It accepts a required child Widget parameter. It renders the star_tessellation_frame_full.svg using SvgPicture.asset with colorFilter set to ColorFilter.mode using the borderStroke color from Theme.of(context).extension of BurdahColors and BlendMode.srcIn. Use the fit parameter BoxFit.contain. Layer the SVG behind or around the child using a Stack, with the child inset by the spacing-lg value (24px per UI-SPEC) from the border edges using EdgeInsetsDirectional. Do NOT use the deprecated color or colorBlendMode parameters on SvgPicture (per RESEARCH.md Pitfall 6 — these were removed from flutter_svg 2.3.0).

    Create lib/widgets/geometric_card_frame.dart following the same pattern as GeometricBorderFrame but using star_tessellation_frame_card.svg and spacing-sm (8px per UI-SPEC) for the content inset. Name the widget GeometricCardFrame, accepting a required child Widget.

    Create lib/widgets/gold_cta_button.dart as a StatelessWidget named GoldCtaButton. It accepts required String label and required VoidCallback onPressed parameters. Render as an ElevatedButton (or a custom InkWell-based button) with: background fill color from BurdahColors.gold via Theme.of(context).extension of BurdahColors, label text color from AppColors (the text-on-gold value: 0xFF1B2B20 in both themes per UI-SPEC), and text style using the Heading role (24px Bold per UI-SPEC Typography — this ensures the label is at least 18px, satisfying the WCAG large-text threshold since gold-on-green contrast is only 3.0-4.2:1 per RESEARCH.md Pitfall 4). Use EdgeInsetsDirectional for internal padding (horizontal xl 32px, vertical md 16px per UI-SPEC Spacing Scale). Apply a rounded corner radius.

    Update lib/screens/design_system_test_screen.dart to integrate all three new widgets into the existing test screen. Add a new section that: (1) wraps a portion of the screen content in a GeometricBorderFrame to demonstrate the full-screen border, (2) shows at least one sample card wrapped in a GeometricCardFrame containing a burdah list entry preview (title from catalog data), (3) places a GoldCtaButton at the bottom with the label from UI-SPEC Copywriting Contract (the primary CTA text) and an onPressed that is a no-op (just a print statement — this button is a design verification element, not a functional navigation element until Phase 3). Ensure the updated screen remains scrollable and that all new elements render correctly in both light and dark themes.

    Run flutter analyze and confirm zero errors. Visually verify on a running emulator/simulator that the geometric borders display the star pattern, the gold CTA button shows gold fill with dark text, and toggling dark mode recolors the borders and button correctly.
  </action>
  <verify>
    <automated>cd /Users/0xnormii/Desktop/BurdahApp && flutter analyze 2>&1 | grep -E 'No issues found|error' | head -5</automated>
  </verify>
  <acceptance_criteria>
    - flutter analyze exits with zero errors
    - assets/images/svg/star_tessellation_frame_full.svg exists and contains valid SVG markup with path or polygon elements forming a rectangular border pattern
    - assets/images/svg/star_tessellation_frame_card.svg exists and contains valid SVG markup
    - lib/widgets/geometric_border_frame.dart declares class GeometricBorderFrame extends StatelessWidget, imports flutter_svg, and uses SvgPicture.asset with colorFilter parameter (not color parameter)
    - lib/widgets/geometric_card_frame.dart declares class GeometricCardFrame extends StatelessWidget with the same colorFilter pattern
    - lib/widgets/gold_cta_button.dart declares class GoldCtaButton extends StatelessWidget, reads BurdahColors.gold from Theme.of(context)
    - lib/screens/design_system_test_screen.dart imports GeometricBorderFrame and GeometricCardFrame and GoldCtaButton
    - No widget file contains hardcoded EdgeInsets.only with left or right parameters — all use EdgeInsetsDirectional
    - No widget file contains hardcoded Color literals with 0xFF prefix — all colors are read from Theme.of(context) or AppColors static constants
  </acceptance_criteria>
  <done>Three reusable themed widgets (GeometricBorderFrame, GeometricCardFrame, GoldCtaButton) render on the updated test screen with theme-driven colors in both light and dark modes. The star tessellation borders frame content cleanly without competing with it. The gold CTA button displays with correct fill color and accessible label text size. flutter analyze reports zero errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify complete design system: geometric borders, gold CTA, and full palette</name>
  <read_first>
    - .planning/phases/01-foundation-data-architecture-design-system/01-UI-SPEC.md (Color section for expected visual appearance, Spacing Scale for expected proportions)
    - .planning/phases/01-foundation-data-architecture-design-system/01-RESEARCH.md (Common Pitfalls section: Pitfall 4 for WCAG contrast on gold elements)
    - .planning/phases/01-foundation-data-architecture-design-system/01-CONTEXT.md (D-05 borders frame screens not compete with text, D-06 interlocking star tessellations, D-08 two variants)
  </read_first>
  <what-built>Three reusable design system widgets: (1) GeometricBorderFrame wrapping content in a full-screen Islamic star tessellation border, (2) GeometricCardFrame wrapping individual cards in a smaller tessellation border, (3) GoldCtaButton with gold fill and dark label text. All integrated into the test screen alongside the existing font samples and color swatches from Plan 01.</what-built>
  <how-to-verify>
    1. Launch the app: flutter run (or use a running instance with hot reload)
    2. BORDER CHECK: Find the geometric border frame section. Confirm the border displays a recognizable Islamic star tessellation pattern (interlocking star shapes along the edges). The border should frame the content without filling or obscuring the content area (per D-05 — content area stays clean)
    3. CARD FRAME CHECK: Find the card-sized geometric border. Confirm it wraps the burdah entry preview with a proportionally smaller version of the star tessellation pattern
    4. GOLD CTA CHECK: Find the gold button. Confirm it has a gold/amber fill (not yellow), the text label is readable against the gold background, and the button has appropriate visual weight for a call-to-action
    5. DARK THEME CHECK: Switch to dark mode. Confirm the geometric borders recolor to the dark-theme border stroke color (lighter green), the gold CTA button brightens to the dark-theme gold, and the overall composition remains balanced
    6. SCALE CHECK: If possible, test on both a phone-sized and tablet-sized emulator/simulator. Confirm the full-screen geometric border does not distort, clip, or overflow at different aspect ratios
  </how-to-verify>
  <acceptance_criteria>
    - Geometric borders display recognizable Islamic star tessellation patterns
    - Borders frame content without obscuring or competing with it
    - Card-sized border variant is proportionally appropriate for card content
    - Gold CTA button has readable label text against the gold fill
    - Dark mode correctly recolors borders and button
    - No visual distortion or clipping of borders at the tested screen size
  </acceptance_criteria>
  <resume-signal>Type "approved" to complete Phase 1. If borders need visual adjustment (pattern density, stroke weight, proportions), describe the issue. If gold CTA contrast is problematic, describe what you see.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG asset to flutter_svg parser | Bundled SVG files are parsed by flutter_svg renderer — no user-supplied SVG input exists in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | SVG files loaded via flutter_svg SvgPicture.asset | low | accept | SVGs are bundled at build time, not user-supplied; flutter_svg renders via internal parser without eval or script execution; no attacker-controlled input path exists in v1 |
| T-01-SC | Tampering | pub.dev package installs (flutter_svg already added in Plan 01) | high | mitigate | Package Legitimacy Audit completed in 01-RESEARCH.md — flutter_svg verified as flutter.dev-published package with 2-month-old publish date |
</threat_model>

<verification>
1. flutter analyze exits with zero errors
2. All three widget files exist and import flutter_svg / theme extension as expected
3. Test screen displays all three widgets alongside the existing font and palette sections
4. Dark mode toggle recolors all widgets correctly
5. Human checkpoint confirms visual quality of geometric patterns and gold CTA
</verification>

<success_criteria>
- DSGN-01: Islamic geometric star tessellation patterns render as border frames in both full-screen and card-sized variants, recolored per theme
- Phase 1 success criterion 4: Reusable themed widgets (geometric border, gold CTA button) render correctly and are ready to reuse across screens in Phases 2-4
</success_criteria>

## Artifacts this plan produces

**Classes:** GeometricBorderFrame (StatelessWidget), GeometricCardFrame (StatelessWidget), GoldCtaButton (StatelessWidget)

**Widget parameters:** GeometricBorderFrame.child (Widget), GeometricCardFrame.child (Widget), GoldCtaButton.label (String), GoldCtaButton.onPressed (VoidCallback)

**New files:** lib/widgets/geometric_border_frame.dart, lib/widgets/geometric_card_frame.dart, lib/widgets/gold_cta_button.dart, assets/images/svg/star_tessellation_frame_full.svg, assets/images/svg/star_tessellation_frame_card.svg

**Modified files:** lib/screens/design_system_test_screen.dart (updated to showcase all three widgets)

<output>
Create `.planning/phases/01-foundation-data-architecture-design-system/01-02-SUMMARY.md` when done
</output>
